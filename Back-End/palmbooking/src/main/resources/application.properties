#Port application
server.port=8080

#JPA and database configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# This code is MySQL configuration

spring.datasource.url=${SQL_URl}
spring.datasource.username=${SQL_USER}
spring.datasource.password=${SQL_PASSWORD}

# This code is PostgreSQL configuration production

#spring.datasource.url=${SQL_URL_PG}
#spring.datasource.username=${SQL_USER_PG}
#spring.datasource.password=${SQL_PASSWORD_PG}
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


#Swagger Configurations
springdoc.api-docs.enabled = true
springdoc.swagger-ui.enabled = true
springdoc.swagger-ui.path=/doc/swagger-ui.html
